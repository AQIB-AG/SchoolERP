# SchoolERP — Frontend

Next.js landing page for SchoolManager.

## Run

```bash
npm install
copy .env.example .env.local
npm run dev
```

Open **http://localhost:3000**

Requires backend at `http://localhost:4000` for contact form submissions.
